@page
@model AspNetCoreWebAppCodeFirst.Pages.Manufacturers.DetailsModel
@{
}

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Car</h4>
    <hr />
    @foreach (var item in Model.ListOfCars)
    {

    <dl class="row">
        <dt class="col-sm-2">
            RegNo
        </dt>
        <dd class="col-sm-10">
            @item.RegNo
        </dd>
        <dt class="col-sm-2">
            Year
        </dt>
        <dd class="col-sm-10">
            @item.Year
        </dd>
    </dl>
    }
</div>
<div>
    @*<a asp-page="./Edit" asp-route-id="@Model.Id">Edit</a>*@ |
    <a asp-page="./Index">Back to List</a>
</div>
